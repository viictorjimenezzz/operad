{
  "algorithm": "Debate",
  "version": 1,
  "dataSources": {
    "summary": { "endpoint": "/runs/$context.runId/summary" },
    "debate": {
      "endpoint": "/runs/$context.runId/debate.json",
      "stream": "/runs/$context.runId/debate.sse"
    },
    "children": { "endpoint": "/runs/$context.runId/children" },
    "agentsSummary": { "endpoint": "/runs/$context.runId/agents-summary" }
  },
  "spec": {
    "root": "page",
    "elements": {
      "page": {
        "type": "Tabs",
        "props": {
          "tabs": [
            { "id": "rounds", "label": "Rounds" },
            { "id": "transcript", "label": "Transcript" },
            { "id": "consensus", "label": "Consensus" },
            {
              "id": "agents",
              "label": "Agents",
              "badge": "$expr:count($queries.agentsSummary.agents)",
              "condition": "$expr:count($queries.agentsSummary.agents)"
            },
            { "id": "events", "label": "Events", "badge": "$queries.summary.event_total" }
          ]
        },
        "children": ["rounds", "transcript", "consensus", "agents", "events"]
      },
      "rounds": {
        "type": "DebateRoundsTab",
        "props": { "source": "$queries.debate" }
      },
      "transcript": {
        "type": "DebateTranscriptTab",
        "props": { "source": "$queries.debate" }
      },
      "consensus": {
        "type": "DebateConsensusTab",
        "props": { "source": "$queries.debate" }
      },

      "agents": { "type": "AgentsTab", "props": { "runId": "$context.runId", "groupBy": "none" } },
      "events": { "type": "EventsTab", "props": { "runId": "$context.runId" } }
    }
  }
}
