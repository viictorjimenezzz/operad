/**
 * Public API surface for layout authors (iter-3 agents).
 * Import from here rather than directly from lib/layout-schema.
 */
export type { DataSourceSpec, ElementSpec, LayoutSpec } from "@/lib/layout-schema";
export { resolvePath } from "@/lib/layout-schema";
